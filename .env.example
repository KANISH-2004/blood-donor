# Used by docker-compose.yml for the Postgres container.
# Copy to .env at the project root when running docker compose.
POSTGRES_USER=blood_donor_user
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=blood_donor_db

# See backend/.env.example and frontend/.env.example for service-specific variables.
